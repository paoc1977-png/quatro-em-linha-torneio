function render(s){$('#title').textContent=s.config.title;$('#round').textContent=s.status==='setup'?'Inscrições':s.status==='finished'?'Final':`Ronda ${s.currentRound}/${s.config.rounds}`;const r=s.rounds[s.currentRound-1];$('#matches').innerHTML=r?r.matches.map(m=>`<div class="row"><span><i class="dot red"></i>${esc(pname(m.red))}</span><b>${m.bye?'Isento':m.status==='finished'?(m.draw?'Empate':'Venceu '+esc(pname(m.winner))):'Em jogo'}</b><span class="right">${m.yellow?esc(pname(m.yellow))+' <i class="dot yellow"></i>':''}</span><span>${m.status==='finished'?'✓':'●'}</span></div>`).join(''):'A aguardar o início.';$('#standings').innerHTML=`<table><thead><tr><th>#</th><th>Jogador</th><th>Pts</th><th>Vit.</th></tr></thead><tbody>${s.standings.map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.name)}</td><td><b>${p.points}</b></td><td>${p.wins}</td></tr>`).join('')}</tbody></table>`}events(render);
