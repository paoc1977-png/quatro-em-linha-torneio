import*as THREE from'/vendor/three.module.min.js';export class SpatialBoard{constructor(host,ui,play){this.host=host;this.ui=ui;this.play=play;this.enabled=false;this.az=.74;this.el=.48;this.dist=8.2;this.scene=new THREE.Scene();this.camera=new THREE.PerspectiveCamera(38,1,.1,100);this.renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});this.renderer.setPixelRatio(Math.min(devicePixelRatio,2));this.renderer.shadowMap.enabled=true;host.appendChild(this.renderer.domElement);this.scene.add(new THREE.HemisphereLight(0xfff7e8,0x243247,2.1));const light=new THREE.DirectionalLight(0xffffff,3.2);light.position.set(-4,8,5);light.castShadow=true;this.scene.add(light);this.root=new THREE.Group();this.scene.add(this.root);const base=new THREE.Mesh(new THREE.BoxGeometry(4.2,.36,4.2),new THREE.MeshPhysicalMaterial({color:0x183963,metalness:.45,roughness:.22,clearcoat:1}));base.position.y=-.18;this.root.add(base);const pg=new THREE.CylinderGeometry(.055,.065,3.62,20),pm=new THREE.MeshStandardMaterial({color:0xdbe5eb,metalness:.9,roughness:.18}),sg=new THREE.CylinderGeometry(.13,.13,.09,24),sm=new THREE.MeshStandardMaterial({color:0x7fa1bf,metalness:.7,roughness:.25});this.selectors=[];for(let x=0;x<4;x++)for(let z=0;z<4;z++){const px=(x-1.5)*.88,pz=(z-1.5)*.88,s=new THREE.Mesh(sg,sm);s.position.set(px,.08,pz);this.root.add(s);const p=new THREE.Mesh(pg,pm);p.position.set(px,1.88,pz);this.root.add(p);const pick=new THREE.Mesh(new THREE.TorusGeometry(.19,.035,10,32),new THREE.MeshBasicMaterial({transparent:true,opacity:0}));pick.position.set(px,3.84,pz);pick.userData={x,z};this.selectors.push(pick);this.root.add(pick)}this.pieces=new THREE.Group();this.root.add(this.pieces);this.target=new THREE.Vector3(0,1.55,0);this.pointer=new THREE.Vector2();this.ray=new THREE.Raycaster();let drag=false,moved=false,lx=0,ly=0,dx=0,dy=0;const canvas=this.renderer.domElement;canvas.onpointerdown=e=>{drag=true;moved=false;lx=dx=e.clientX;ly=dy=e.clientY;canvas.setPointerCapture(e.pointerId)};canvas.onpointermove=e=>{if(!drag)return;if(Math.hypot(e.clientX-dx,e.clientY-dy)>5)moved=true;if(moved){this.az-=(e.clientX-lx)*.009;this.el=Math.max(.18,Math.min(1.18,this.el+(e.clientY-ly)*.007));this.cameraPos()}lx=e.clientX;ly=e.clientY};canvas.onpointerup=e=>{if(!moved&&this.enabled){const r=canvas.getBoundingClientRect();this.pointer.set((e.clientX-r.left)/r.width*2-1,-((e.clientY-r.top)/r.height*2-1));this.ray.setFromCamera(this.pointer,this.camera);const h=this.ray.intersectObjects(this.selectors)[0]?.object;if(h)this.play(h.userData.x,h.userData.z)}drag=false};canvas.onwheel=e=>{e.preventDefault();this.dist=Math.max(6.2,Math.min(11,this.dist+e.deltaY*.006));this.cameraPos()};ui.near.onclick=()=>{this.dist=Math.max(6.2,this.dist-.7);this.cameraPos()};ui.far.onclick=()=>{this.dist=Math.min(11,this.dist+.7);this.cameraPos()};ui.home.onclick=()=>{this.az=.74;this.el=.48;this.dist=8.2;this.cameraPos()};new ResizeObserver(()=>this.resize()).observe(host);this.cameraPos();this.resize();const draw=()=>{requestAnimationFrame(draw);this.renderer.render(this.scene,this.camera)};draw()}cameraPos(){const h=Math.cos(this.el)*this.dist;this.camera.position.set(Math.sin(this.az)*h,this.target.y+Math.sin(this.el)*this.dist,Math.cos(this.az)*h);this.camera.lookAt(this.target)}resize(){const r=this.host.getBoundingClientRect();this.renderer.setSize(r.width,r.height,false);this.camera.aspect=r.width/Math.max(1,r.height);this.camera.updateProjectionMatrix()}update(cells,enabled){this.enabled=enabled;this.pieces.clear();for(const c of cells){const b=new THREE.Mesh(new THREE.SphereGeometry(.34,30,20),new THREE.MeshPhysicalMaterial({color:c.colour==='red'?0xe83f52:0xffc928,roughness:.19,clearcoat:1}));b.position.set((c.x-1.5)*.88,.38+c.y*.76,(c.z-1.5)*.88);this.pieces.add(b)}this.ui.map.innerHTML=Array.from({length:4},(_,z)=>Array.from({length:4},(_,x)=>{const n=cells.filter(c=>c.x===x&&c.z===z).length;return `<button data-x="${x}" data-z="${z}" ${!enabled||n===4?'disabled':''}>${n||''}</button>`}).join('')).join('');this.ui.map.querySelectorAll('button').forEach(b=>b.onclick=()=>this.play(+b.dataset.x,+b.dataset.z))}}
