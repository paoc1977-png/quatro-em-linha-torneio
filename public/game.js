import{SpatialBoard}from'/spatial.js';let token=localStorage.fourToken||('d-'+Date.now()+'-'+Math.random().toString(36).slice(2)),me=localStorage.fourPlayer||'',table=localStorage.fourTable||'',board3;localStorage.fourToken=token;const room=new URLSearchParams(location.search).get('room')||localStorage.fourRoom||'';$('#roomInput').value=room;function ranking(s){return `<table><tbody>${s.standings.map((p,i)=>`<tr><td>${i+1}</td><td>${esc(p.name)}</td><td><b>${p.points}</b></td></tr>`).join('')}</tbody></table>`}async function joinPlayer(id){try{await post('/api/join',{roomCode:$('#roomInput').value,playerId:id,token});me=id;localStorage.fourPlayer=id;localStorage.fourRoom=$('#roomInput').value}catch(e){$('#error').textContent=e.message}}async function joinTable(id){try{await post('/api/join-table',{roomCode:$('#roomInput').value,matchId:id,token});table=id;localStorage.fourTable=id;localStorage.fourRoom=$('#roomInput').value}catch(e){$('#error').textContent=e.message}}async function drop(data){try{await post('/api/drop',{...data,token})}catch(e){$('#error').textContent=e.message}}
function render(s){state=s;const r=s.rounds[s.currentRound-1],shared=s.config.deviceMode==='shared',mine=shared?r?.matches.find(m=>m.id===table):r?.matches.find(m=>m.red===me||m.yellow===me);$('#ranking').innerHTML=ranking(s);if(!mine){$('#game').classList.add('hidden');$('#waiting').classList.add('hidden');$('#join').classList.remove('hidden');$('#joinTitle').textContent=shared?'Escolham a vossa partida':'Escolhe o teu nome';$('#choices').innerHTML=shared?(r?r.matches.filter(m=>!m.bye).map(m=>`<button data-table="${m.id}" ${m.sharedClaimed?'disabled':''}><i class="dot red"></i>${esc(pname(m.red))} vs. ${esc(pname(m.yellow))}<i class="dot yellow"></i></button>`).join(''):'A aguardar o início.'):s.players.map(p=>`<button data-player="${p.id}" ${p.claimed?'disabled':''}>${esc(p.name)}${p.claimed?' · já ligado':''}</button>`).join('');document.querySelectorAll('[data-player]').forEach(b=>b.onclick=()=>joinPlayer(b.dataset.player));document.querySelectorAll('[data-table]').forEach(b=>b.onclick=()=>joinTable(b.dataset.table));return}if(mine.bye){$('#join').classList.add('hidden');$('#game').classList.add('hidden');$('#waiting').classList.remove('hidden');return}$('#join').classList.add('hidden');$('#waiting').classList.add('hidden');$('#game').classList.remove('hidden');$('#round').textContent=`Ronda ${s.currentRound} de ${s.config.rounds} · ${s.config.variant==='spatial'?'Espacial 4×4×4':'Clássico 7×6'}`;$('#versus').innerHTML=`<i class="dot red"></i>${esc(pname(mine.red))} vs. ${esc(pname(mine.yellow))}<i class="dot yellow"></i>`;const can=mine.status==='playing'&&(shared||mine.turn===me);$('#turn').textContent=mine.status==='finished'?(mine.draw?'Empate':`Venceu ${pname(mine.winner)}`):can?`Agora joga ${pname(mine.turn)}`:'Aguarda a jogada do adversário';if(s.config.variant==='classic'){$('#classic').classList.remove('hidden');$('#spatial').classList.add('hidden');$('#classic').innerHTML=mine.board.flatMap((row,ri)=>row.map((c,ci)=>`<button class="cell ${c||''}" data-col="${ci}" ${can?'':'disabled'} aria-label="Coluna ${ci+1}"></button>`)).join('');document.querySelectorAll('[data-col]').forEach(b=>b.onclick=()=>drop({matchId:mine.id,column:+b.dataset.col}))}else{$('#classic').classList.add('hidden');$('#spatial').classList.remove('hidden');if(!board3)board3=new SpatialBoard($('#three'),{near:$('#near'),far:$('#far'),home:$('#home'),map:$('#map')},(x,z)=>drop({matchId:board3.matchId,x,z}));board3.matchId=mine.id;board3.update(mine.cells,can)}}events(render);
